machine:
  services:
    - docker

dependencies:
  pre:
    - sudo apt-get clean && sudo apt-get install -y && sudo apt-get update -y
    - docker --version
    - git clone https://github.com/sstephenson/bats.git

test:
  post:
    - docker build --rm=true -t frekele/helicopterizer .

