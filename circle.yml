machine:
  services:
    - docker
  pre:
    - sudo curl -sSL https://get.docker.com/ | sh

dependencies:
  pre:
    - sudo apt-get update -y
    - docker --version
    - git clone https://github.com/sstephenson/bats.git

test:
  post:
    - docker build --rm=true -t frekele/helicopterizer .

