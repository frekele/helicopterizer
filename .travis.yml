sudo: required

services:
  - docker

before_install:
  - docker -v
  - git clone https://github.com/sstephenson/bats.git

script:
  - docker build --rm=true -t $DOCKER_IMAGE_NAME .

env:
  - DOCKER_IMAGE_NAME=frekele/helicopterizer

